version: '3.8'
services:
  h2:
    image: oscarfonts/h2  # H2 데이터베이스 이미지
    container_name: h2-database
    ports:
      - "1521:1521"  # H2 TCP 포트
      - "8082:8082"  # H2 콘솔 포트
    environment:
      H2_OPTIONS: "-ifNotExists"
    volumes:
      - h2-data:/opt/h2-data
volumes:
  h2-data:
